#ifndef __MDN_utils_HPP__
#define __MDN_utils_HPP__

// namespace mdn {

    enum MPI_TAGS : int
    {
        SANITY,
        STATE
    };

    enum RETURN_CODES : int
    {
        OK,
        ERROR,
        EXIT
    };

// } // namespace mdn

#endif // !__MDN_utils_HPP__